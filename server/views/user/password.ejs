<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <center>
        <% if (pass == false) { %>
            <h1>Masukan Password Anda Saat Ini</h1>

            <% if (typeof errors !== 'undefined') { %>
                <h2 style="color: red;">! <%= errors %></h2>
            <% } %>

            <form action="/user/profil/password" method="post">
                <input type="password" id="password" name="password" placeholder="Password" required> <br>
                <br> <button type="submit">Kirim</button>
            </form>
        <% } %>

        <% if (pass == true) { %>
            <h1>Masukan Password Baru</h1>

            <% if (typeof errors !== 'undefined') { %>
                <h2 style="color: red;">! <%= errors %></h2>
            <% } %>

            <form action="/user/profil/password/baru" method="post">
                <input type="password" id="password" name="password" placeholder="Password" required> <br>
                <br> <button type="submit">Kirim</button>
            </form>
        <% } %>

        <br> <a href="/user/profil">kembali</a>
    </center>
</body>
</html>