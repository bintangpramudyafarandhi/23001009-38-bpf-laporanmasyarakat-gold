<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Info Petugas</h1>

    <h3>Username: <%= data[0].username %></h3>
    <h3>Nama Lengkap: <%= data[0].nama %></h3>
    <h3>Nomor Telepon: <%= 0+data[0].no_telp %></h3>
    <h3>Alamat: <%= data[0].alamat %></h3> <br>

    <h3>Waktu Dibuat: <%= data[0].created_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3>
    <h3>Terakhir Diubah: <%= data[0].updated_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3> <br>

    <a href="/admin/<%= id %>/petugas">kembali</a>
</body>
</html>