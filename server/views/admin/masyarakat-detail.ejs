<style>
    .card-header {
        background-color: #2E3691;
        color: white;
        font-size: 30px;
        font-weight: 700;
    }

    .key {
        font-size: 20px;
    }

    span {
        font-weight: 600;
    }

    .card {
        border-radius: 20px !important;
    }

    .card-header {
        border-top-right-radius: 20px !important;
        border-top-left-radius: 20px !important;
    }

    .card-footer {
        border-bottom-right-radius: 20px !important;
        border-bottom-left-radius: 20px !important;
        background-color: #2E3691;
    }

    .prof-cont {
        margin-top: 80px !important;
    }
</style>

<body>
    <!-- <h1>Info Masyarakat</h1>

    <h3><b>NIK:</b> <%= data[0].nik %></h3>
    <h3><b>Nama Lengkap:</b> <%= data[0].nama %></h3>
    <h3><b>Nomor Telepon:</b> <%= 0+data[0].no_telp %></h3>
    <h3><b>Alamat:</b> <%= data[0].alamat %></h3> <br>

    <h3><b>Waktu Dibuat:</b> <%= data[0].created_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3>
    <h3><b>Terakhir Diubah:</b> <%= data[0].updated_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3> <br> -->
    
    <div class="container w-75 prof-cont">
        <div class="card">
            <div class="card-header text-center">
                Info Masyarakat
            </div>
            <div class="card-body">
                <p class="key mb-1"><span>NIK:</span> <%= data[0].nik %></p>
                <p class="key mb-1"><span>Nama Lengkap:</span> <%= data[0].nama %></p>
                <p class="key mb-1"><span>Nomor Telepon:</span> <%= 0+data[0].no_telp %></p>
                <p class="key mb-1"><span>Alamat:</span> <%= data[0].alamat %></p>

                <p class="key mt-5"><span>Waktu Dibuat:</span> <%= data[0].updated_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></p>
                <p class="key mt-1"><span>Terakhir Diubah:</span> <%= data[0].updated_at.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></p>
            </div>
            <div class="card-footer">
            </div>
        </div>
    </div>
</body>
</html>