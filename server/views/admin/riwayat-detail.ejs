<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Info Laporan</h1>

    <h3>Nama Pengirim: <%= data.nama %></h3>
    <h3>NIK: <%= data.nik %></h3>
    <h3>Tanggal Masuk: <%= data.tgl_laporan.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3>
    <h3>Isi laporan: <%= data.isi_laporan %></h3> <br>

    <h1>Balasan</h1>

    <h3>Isi Balasan: <%= data.isi_balasan %></h3>
    <h3>Dibalas Oleh: <%= dataAdmin.nama %></h3>
    <h3>Nomor Telepon: <%= 0+dataAdmin.no_telp %></h3>
    <h3>Alamat: <%= dataAdmin.alamat %></h3>
    <h3>Dibalas Pada: <%= data.tgl_balasan.toLocaleDateString('id-ID', { weekday: 'long', day: '2-digit', month: 'long', year: 'numeric', hour: '2-digit', minute: '2-digit', timeZoneName: 'short' }) %></h3>

    <br> <br> <a href="/admin/<%= id %>/riwayat">kembali</a>
</body>
</html>